# Scale evidence

Generated by `ci/scale_report.py` from CI run
[31918763850](https://github.com/berseru/blastradius/actions/runs/31918763850)
(commit `afba1ce`, levels `143 300 600`). Raw `summary.json`, `ingest-*.json`
and `verify-*.json` are attached to that run as the `scale-artifacts` artifact.

## Results

The same pipeline, run four times against a fresh HydraDB store, on a
seed list where every level is a prefix of the next one. Query times are
the median across services, measured as round-trip time to the database.

| seeds | packages | versions | nodes | edges | ingest (s) | direct_hits (ms) | depth_profile (ms) | choke_points (ms) | exposure_windows (ms) | lookalikes (ms) | blast_radius (ms) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 143 | 2,389 | 3,113 | 6,705 | 16,264 | 1,289 | 42 | 882 | 326 | 26 | 90 | 24 |
| 300 | 4,265 | 5,606 | 11,898 | 31,201 | 1,830 | 54 | 1,208 | 455 | 30 | 124 | 35 |
| 600 | 5,867 | 7,869 | 16,412 | 44,219 | 1,669 | 87 | 962 | 363 | 23 | 130 | 38 |

From 143 to 600 seeds the graph grew 2.5x in package versions while the slowest query grew 1.1x. Every level ran every query with 0 query failures.

Raw numbers: `artifacts/scale/summary.json`, and the per-level `ingest-*.json` / `verify-*.json` they were read from, are attached to the `scale` workflow run.
